[build-system]
requires = [
  "scikit-build-core>=0.8",
  "pybind11>=2.11"
]
build-backend = "scikit_build_core.build"

[project]
name = "cuDCT"
version = "0.1.0"
requires-python = ">=3.9"

[tool.scikit-build]
wheel.packages = ["python/cuDCT"]
